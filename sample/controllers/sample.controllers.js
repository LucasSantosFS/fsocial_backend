const sampleModel = require('../models/sample.models');

exports.getAll = (req, res) => {
    res.status(501).json({
        status: "failure",
        message: "not implemented yet",
        payload: ""
    });
};